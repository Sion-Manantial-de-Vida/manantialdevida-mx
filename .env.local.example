# ============================================================
#  PLANTILLA DE TOKENS  ·  Iglesia Sion Manantial de Vida
# ============================================================
# Este archivo SÍ se sube a GitHub (no tiene secretos reales).
# Copia este archivo como ".env.local" y pon ahí tus tokens REALES.
# ".env.local" NUNCA se sube (está protegido por .gitignore).
# ============================================================

# --- GitHub (acceso a repositorios) ---
GITHUB_TOKEN=ghp_tu_token
GH_TOKEN=ghp_tu_token

# --- Vercel (hosting / CLI) ---
# Crear en: https://vercel.com/account/tokens
VERCEL_TOKEN=vcp_tu_token

# --- Supabase (base de datos / CLI / Management API) ---
# Crear en: https://supabase.com/dashboard/account/tokens
SUPABASE_ACCESS_TOKEN=sbp_tu_token
SUPABASE_PROJECT_REF=wisgwbfzyusdwlotspqn

# (La clave pública del navegador NO va aquí; está en supabase-config.js)
